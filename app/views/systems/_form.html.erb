<%= bootstrap_form_for(@system) do |f| %>
  <%= f.alert_message "Verifique os erros abaixo antes de prosseguir:" %>

    <%= f.text_field :nome %>
    <%= f.text_field :responsavel %>
    
   <div align="center">
     <%= f.submit %>
     <%= link_to 'Voltar', systems_path , class: "btn btn-default"%>
    </div>
<% end %>
